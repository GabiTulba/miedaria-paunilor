================================================================================
MIEDARIA PAUNILOR — OUTSTANDING WORK
Last updated: 2026-05-10
================================================================================

Severity legend: [C]ritical  [H]igh  [M]edium  [L]ow

Each item: [Sev] FILE:LINE — Issue. Fix.

================================================================================
PRIORITY
================================================================================
1. [H] backend + frontend — JWT to httpOnly cookie (requires backend cookie set on
   /api/admin/login; FE side now decodes exp + auto-logs out on expiry. Migration to
   cookie still pending and needs FE+BE in lockstep).
2. [M] frontend/main.tsx + nginx — mirror root `/` lang redirect at nginx level
   (FE-side redirect is intentionally kept as fallback).

================================================================================
D. BACKEND — DEFERRED FOLLOW-UPS
================================================================================
* `db::establish_connection` / `establish_pooled_connection` — left in place;
  `establish_connection` is still used by `bin/add_admin_user.rs`.
* Test coverage — codebase has none; introducing a Postgres-test-container
  harness would multiply the work. Recommend follow-up.

================================================================================
E. FRONTEND — DEFERRED FOLLOW-UPS
================================================================================

Form layer (deferred — no test coverage to fall back on)
[H] frontend/src/pages/admin/ProductForm.tsx + AdminProductCreate.tsx +
    AdminProductEdit.tsx + BlogForm.tsx — No shared form-state primitive.
    Each page hand-rolls handleChange/setFormData. Introduce a tiny
    `useForm<T>()` hook (or pull in `react-hook-form`) so the boilerplate
    disappears and the dirty-tracking that `useUnsavedChanges` already
    supports can be driven off `formState.isDirty`.
[H] frontend/src/pages/admin/ProductForm.tsx — once RHF lands, split into
    sub-form components (BasicInfoSection, ImageSection, CharacteristicsSection,
    PricingSection) using `useFormContext`. Skipped pre-RHF because without
    a shared form context the split adds prop drilling without value.
[M] frontend/src/pages/admin/ProductForm.tsx — `handleChange` and
    `handleNumericChange` are two parallel handlers. With NumberInput emitting
    a `number` via a separate `onValueChange` prop, the page only needs one
    `handleChange`.
[L] frontend/src/pages/admin/errorMappings.ts — `errorMessageMapping` is a
    flat object of English strings. Translate via i18n keys at the call site
    or move strings into the i18n resource files.

Hooks / data fetching
[M] frontend/src/hooks/useAdminImages.ts — Per-component fetch with no cache.
    `AdminImages`, `AdminProductCreate`, and `AdminProductEdit` instantiate it
    independently → 3 GET /admin/images on a single navigation flow. Consider
    an `ImagesContext` or a small SWR-style cache keyed on `token`.
[M] frontend/src/pages/admin/AdminProductEdit.tsx — Edit page handles
    `productLoading` separately from `imagesLoading` (useAdminImages); they're
    combined ad hoc. Move both single-shot loaders to the `useFetch<T>` hook
    so they aggregate cleanly.
[L] frontend/src/context/EnumContext.tsx:28-34 — Validates the API response
    shape inline. Move to a `validateEnumResponse(...)` helper next to the
    EnumValues type definition.

Components / structure
[L] frontend/src/i18n/locales/index.d.ts and JSON files — typed-key wrapper
    over EN/RO key trees (codegen or hand-written `Keys` union). Skipped this
    pass — needs tooling decision.
[L] frontend/src/pages/admin/AdminLayout.tsx + frontend/src/App.tsx — Both
    implement a hamburger/drawer pattern. JS is now shared via
    `useFocusTrapDrawer`; an additional `<Drawer>` component wrapping the
    markup would also collapse the markup duplication.
[M] frontend/src/pages/Cart.tsx + ProductDetails.tsx — three hand-rolled
    `success-pulse`/`onanimationend` snippets. Could be unified via a
    `<PulseButton>` or `useElementPulse(ref)` hook; left as-is because the
    existing setTimeout-driven approach is already idiomatic React.

================================================================================
F. CROSS-CUTTING — FE/BE (OUTSTANDING)
================================================================================

Backend codegen / API contract sharing — all blocked on a single tooling
decision (typeshare vs ts-rs vs OpenAPI). Pick one and the next four items
collapse into "regenerate".

[H] backend/src/enums.rs (7 enums) vs frontend/src/enums.ts +
    i18n resources — Each enum's variants live in three places (Rust enums,
    TS mapping, i18n JSON). Adding a mead style requires touching all three.
    Generate the FE enum file from BE, or expose `/api/enums` as the source
    of truth and remove the FE static type.
[H] backend/src/product_crud.rs (ProductValidationError variants) vs
    frontend/src/pages/admin/errorMappings.ts — Two truth tables; drift has
    already started (FE `InvalidTaninsType` single-n vs BE `TanninsType`
    double-n). Generate or share the variant list.
[M] backend/src/models.rs (Image, Product, BlogPost) vs
    frontend/src/types/models.ts — DTOs are duplicated (field names match;
    nullability sometimes differs). Use codegen (typeshare/ts-rs/OpenAPI) so
    models stay in sync.
[M] backend/src/main.rs (`GetProductsQuery`) vs frontend/src/lib/api.ts
    (`getProducts` params type) — Filter parameters typed in both places.
    Same codegen suggestion.
[M] frontend/src/types/models.ts (`Product` vs `LocalizedProduct`) —
    Public `/api/products` returns `LocalizedProduct`, admin returns full
    `Product`. Both live in the same module and are used in different code
    paths. Document the contract or split into `api.public.*` vs `api.admin.*`
    clients.

================================================================================
